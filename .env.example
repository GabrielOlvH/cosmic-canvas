# OpenAI API Key (optional - can be entered in the UI)
# Get your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Chroma vector database URL (defaults to http://localhost:8000)
# Update this if you're running Chroma on another host/port or via Chroma Cloud
CHROMA_URL=http://localhost:8000
